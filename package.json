{
  "name": "duty-roster",
  "version": "1.0.0",
  "description": "Haryana Police Duty Management UI (Naukri Chitha) — static, localStorage-backed",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/index.html && cp -r public/* dist/ 2>/dev/null || true",
    "start": "npx serve ."
  }
}
